#include <stdio.h>
#include <math.h>

int run(void) {
  // printf("Hello, world! (from t7_math.c)");

  // int x = 2;
  // x = sqrt(x);
  // x = pow(x, 3);
  // printf("%d", x);

  // float x = -3.0f;
  // x = round(x);
  // x = ceil(x);
  // x = floor(x);
  // printf("%f", x);

  // int x = -3;
  // x = abs(x);
  // printf("%d", x);

  float x = 45; // 3;
  // x = log(x);
  // x = sin(x);
  // x = cos(x);
  x = tan(x);
  printf("%f", x);


  return 0;
}
