#include <stdio.h>

int run(void) {
  // printf("Hello, world! (t3_arithmetic_operators.c)\n");

  // int x = 2, y = 3, z = 0;
  // char symbol = '+';
  // char symbol = '-';
  // char symbol = '*';
  // z = x + y;
  // z = x - y;
  // z = x * y;
  // printf("%d %c %d = %d", x, symbol, y, z);

  // float x = 2, y = 3, z = 0;
  // char symbol = '/';
  // z = x / y;
  // printf("%.f %c %.f = %.4f", x, symbol, y, z);

  // int x = 11/*10*/, y = 2/* 3 */, z = 0;
  // char symbol = '%';
  // z = x % y;
  // printf("%d %c %d = %d", x, symbol, y, z);

  int x = 10;
  // x++;
  // x--;
  // x = x + 2;
  // x += 2;
  // x -= 2;
  // x *= 2;
  // x /= 2;
  x %= 3;
  printf("%d", x);
  
  return 0;
}
